streamlit>=1.57.0
pyyaml>=6.0.2
python-dotenv>=1.1.0
rapidfuzz>=3.14.5

# Embeddings
sentence-transformers>=3.0.0,<6.0.0

# LlamaIndex — pinned to a consistent compatible set
# llama-index-llms-groq 0.3.x requires llama-index-llms-openai<0.7
# so we stay on llama-index-llms-openai 0.6.x
llama-index-core>=0.14.0,<0.15.0
llama-index-embeddings-huggingface>=0.4.0,<1.0.0
llama-index-llms-groq>=0.3.0,<0.6.0
llama-index-llms-openai>=0.6.0,<0.7.0

# chromadb is only needed locally (persisted vector store).
# On Streamlit Cloud the app automatically uses an in-memory index instead.
# Install locally with: pip install chromadb llama-index-vector-stores-chroma
